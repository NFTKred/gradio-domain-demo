# Direct dependencies only; pip resolves the rest.
# Verified on Python 3.14.2.
#
#   python -m venv .venv
#   .venv/Scripts/python.exe -m pip install -r requirements.txt

# Pinned deliberately: Gradio 6 dropped the Chatbot(type=...) argument that
# earlier versions required, so the major version is not safe to float.
gradio==6.24.0

# Needs mcp-client-2025-11-20 beta support and the claude-opus-5 model.
anthropic==0.122.0

python-dotenv==1.2.3
